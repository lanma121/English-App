Mac
1. brew install portaudio
2. python3 -m pip install --upgrade pip  
3. pip3 install pyaudio SpeechRecognition

<!-- Capturing system audio output (e.g., music, app sounds, browser audio) instead of microphone input  -->